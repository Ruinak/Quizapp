<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cos.quizapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Mr. Puzzling"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quizapp">
        <activity android:name=".activity.knowledge.KnowledgeSelectActivity" />
        <activity android:name=".activity.knowledge.KnowledgeActivity" />
        <activity android:name=".activity.inference.InferenceSelectActivity" />
        <activity android:name=".activity.inference.InferenceActivity" />
        <activity android:name=".activity.forword.ForWordSelectActivity" />
        <activity android:name=".activity.forword.ForWordActivity" />
        <activity android:name=".activity.GradeActivity" />
        <activity android:name=".activity.user.UserListActivity" />
        <activity android:name=".activity.user.UserInfoActivity" />
        <activity android:name=".activity.user.PasswordResetActivity" />
        <activity android:name=".activity.user.UserInitActivity" />
        <activity android:name=".activity.user.LoginActivity" />
        <activity android:name=".activity.user.JoinActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>